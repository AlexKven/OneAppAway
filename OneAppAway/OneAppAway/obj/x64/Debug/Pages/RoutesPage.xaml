<Page x:ConnectionId='1'
    x:Class="OneAppAway.RoutesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneAppAway"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" RequestedTheme="Dark"                               >
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer local:ScrollViewerSnapping.SnapInterval="140" Background="#555555" HorizontalScrollBarVisibility="Visible" HorizontalScrollMode="Enabled" VerticalScrollBarVisibility="Disabled" VerticalScrollMode="Disabled">
            <ListView x:ConnectionId='7' x:Name="AgenciesListView" SelectionMode="Single"                                                     >
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapGrid ItemWidth="140" ItemHeight="60" Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ContentControl HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                            <TextBlock Text="{Binding Name}" TextWrapping="WrapWholeWords"/>
                        </ContentControl>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
        <ScrollViewer Grid.Row="1" HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled" VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:ConnectionId='5' x:Name="MainList" Tag="100">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:RouteListingControl x:ConnectionId='6' Route="{Binding Route}" ShowCheckBox="{Binding ElementName=CheckToggle, Path=IsChecked}" ShowProgress="{Binding Path=ShowProgress}" Progress="{Binding Path=Progress}" IsChecked="{Binding Path=IsChecked, Mode=TwoWay}"                      />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapGrid ItemWidth="{Binding ElementName=MainList, Path=Tag}" Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
        <ProgressRing x:ConnectionId='2' Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="50" Height="50" x:Name="MainProgressRing"/>
        <CommandBar Grid.Row="3">
            <AppBarToggleButton x:ConnectionId='3' Icon="List" Label="Select Items" x:Name="CheckToggle"/>
            <AppBarSeparator/>
            <AppBarButton x:ConnectionId='4' Icon="Download" Label="Download"                             />
        </CommandBar>
    </Grid>
</Page>

